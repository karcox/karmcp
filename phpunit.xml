<?xml version="1.0" encoding="UTF-8"?>
<!--
	KarMCP test suite.

	Upstream shipped two configs: this root one pointed at the private `pro/tests`
	submodule (Pro developers only), and `tests/phpunit.xml` ran the public suite.
	The fork has no private submodule, so this root config now runs the public
	suite directly — `phpunit` works with no arguments.

	Run from the plugin root:  vendor/bin/phpunit
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/10.5/phpunit.xsd"
	bootstrap="tests/bootstrap.php"
	colors="true"
	cacheDirectory=".phpunit.cache"
	beStrictAboutOutputDuringTests="true">

	<testsuites>
		<testsuite name="KarMCP">
			<directory suffix="Test.php">tests</directory>
		</testsuite>
	</testsuites>

	<source>
		<include>
			<directory suffix=".php">includes</directory>
		</include>
		<exclude>
			<directory>tests</directory>
			<directory>vendor</directory>
		</exclude>
	</source>

</phpunit>
